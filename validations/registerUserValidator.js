import { STATUS_CODES } from "../constants/statusCodes";

const validateRegisterUser = async (req, res, next) => {
  const { fistName, lastName, passowrd, confirmPassword, role, company } =
    req.body;

  
};
